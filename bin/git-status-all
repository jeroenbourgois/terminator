git-status-all() {
  for gitdir in `find . -name .git`;
  do
    workdir=$(dirname $gitdir);
    echo;
    echo $workdir;
    git --git-dir=$gitdir --work-tree=$workdir status;
    echo "###########################################"
  done
}
